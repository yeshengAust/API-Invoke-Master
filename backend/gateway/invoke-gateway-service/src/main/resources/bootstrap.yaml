spring:
  application:
    name: invoke-gateway-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 154.219.98.162:8848
      discovery:
        namespace: dev
        group: api
      config:
        namespace: dev
        group: api
        file-extension: yaml
        refresh-enabled: true
      username: nacos
      password: nacos