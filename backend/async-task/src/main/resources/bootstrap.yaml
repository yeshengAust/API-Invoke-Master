spring:
  application:
    name: sendMail-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 115.175.74.221:8848
      discovery:
        namespace: dev
        group: api
      config:
        namespace: dev
        group: api
        file-extension: yaml
        refresh-enabled: true
      username: nacos
      password: nacos
